#include <stdio.h>

int main() {
    // loop are used to make repetative function like to make code to print 1 to 600.
    // we cant make that much code manually so we need to use loop to make that function
    // repetative up to 600 times.

    return 0;
}