#include <stdio.h>

int main() {
    int length, width;

    printf ("type length of rectangle \n");
    scanf ("%d",&length);
   

    printf ("type width of rectangle \n");
    scanf ("%d",&width);
    
    printf ("area of the rectangle is %d",length*width);
   
    return 0;
}