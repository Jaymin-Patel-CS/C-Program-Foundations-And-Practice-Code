#include <stdio.h>

int main() {
    int a ;
    printf ("enter value of a\n");
    scanf ("%d",&a);

    (a<5)? printf("the value is less than five") : printf ("the value is greater than five");

    return 0;
}