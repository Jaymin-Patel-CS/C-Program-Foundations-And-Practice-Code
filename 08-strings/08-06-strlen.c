#include <stdio.h>
#include <string.h>

int main() {
    char *st = "jaymin";
    int a = strlen(st);   // this function is used to calculate the lengeth of string.
    printf ("the length of your string is %d",a);
    return 0;
}