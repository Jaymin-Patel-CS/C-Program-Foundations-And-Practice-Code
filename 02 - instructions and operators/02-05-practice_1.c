// divisibility test.
#include <stdio.h>

int main()
{

    int num;
    printf("enter the number \n");
    scanf ("%d", &num);
    printf ("divisibility test returns : %d",num%5);      //the good code for this
    return 0;
}