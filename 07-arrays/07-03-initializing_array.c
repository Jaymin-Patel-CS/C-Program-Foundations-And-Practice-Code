#include <stdio.h>

int main() {
    int element[] = {1,2,3,4,5};
    for (int i = 0; i < 5; i++)
    {
        printf ("the value of element %d is %d\n",i+1,element[i]);
    }
    
    
    return 0;
}